# 🚀 Node.js API-Datenabruf (API Data Fetching)

## Projektübersicht
Dieses Projekt dient als **praktisches Beispiel** für den asynchronen Datenabruf von externen Web-APIs unter Verwendung von **Node.js** und der **Axios**-Bibliothek. Es demonstriert die korrekte Handhabung von HTTP GET-Anfragen und die Fehlerbehandlung (`try...catch`).

## 🛠️ Verwendete Technologien
* **Node.js** (Laufzeitumgebung)
* **Axios** (HTTP-Client für Browser und Node.js)
* **Async/Await** (Für die saubere Handhabung asynchroner Operationen)

## 🔑 Hauptfunktionalitäten
* Senden einer GET-Anfrage an eine beliebige JSON-API.
* Korrekte Verarbeitung der API-Antwort (Statuscode und Daten).
* Protokollierung der abgerufenen JSON-Daten in der Konsole.

## ⚙️ Installation
Folgen Sie diesen Schritten, um das Projekt lokal einzurichten und auszuführen:

1.  **Repository klonen:**
    ```bash
    git clone [Ihre-Repo-URL]
    cd [Ihr-Projektname]
    ```

2.  **Abhängigkeiten installieren:**
    Stellen Sie sicher, dass Sie Node.js installiert haben.
    ```bash
    npm install
    ```

## ▶️ Ausführung
Starten Sie das Skript mit dem folgenden Befehl:

```bash
node apiFetcher.js
   ```

Wenn Sie mehr erfahren möchten, besuchen Sie https://reservisten-unterfranken.de/
